"""Agentic controller layer.

Will contain the tool-calling controller loop, tool registry, and system
prompt (Phase 6/7). The controller decides *what to investigate*; it never
computes financial facts itself.
"""
