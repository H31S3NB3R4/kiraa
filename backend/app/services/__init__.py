"""Cross-cutting backend services.

Currently home to the synthetic dataset generator (Phase 1), the database
seeding service (Phase 2), the action/approval service (Phase 8 — the
safe write path behind human approval), and the Phase 9 read-only query
and metrics aggregation services behind the dashboard APIs.
"""

