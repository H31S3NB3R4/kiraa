"""Cross-cutting backend services.

Currently home to the synthetic dataset generator (Phase 1), the database
seeding service (Phase 2), and the action/approval service (Phase 8 —
the safe write path behind human approval). Later phases add metrics
aggregation.
"""

